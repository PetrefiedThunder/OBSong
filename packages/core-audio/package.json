{
  "name": "@toposonics/core-audio",
  "version": "0.1.0",
  "description": "Audio mapping utilities for TopoSonics - converts image features to musical events",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p .",
    "dev": "tsc -p . --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tonejs/midi": "^2.0.30",
    "@toposonics/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "publishConfig": {
    "access": "restricted"
  }
}

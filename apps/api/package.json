{
  "name": "@toposonics/api",
  "version": "0.1.0",
  "description": "Fastify backend API for TopoSonics",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p .",
    "start": "node dist/server.js",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "node -e \"console.log('no tests')\""
  },
  "dependencies": {
    "@toposonics/types": "workspace:*",
    "@supabase/supabase-js": "^2.45.0",
    "@fastify/cors": "^8.5.0",
    "@fastify/helmet": "^11.1.1",
    "@fastify/rate-limit": "^9.1.0",
    "@fastify/under-pressure": "^8.3.0",
    "dotenv": "^16.4.5",
    "fastify": "^5.7.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "pino-pretty": "^11.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
